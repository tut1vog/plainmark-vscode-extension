> > [!NOTE]
> > Nested callout — the OUTER Blockquote should get callout chrome; the inner Blockquote does not stack additional callout chrome on top (parent-is-Blockquote short-circuit).

> Plain outer blockquote.
> > [!WARNING]
> > Inner Blockquote: does NOT get callout chrome (matches GitHub "cannot be nested"). Outer renders as plain blockquote at depth 1; inner renders as plain blockquote at depth 2.
