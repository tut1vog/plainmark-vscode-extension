> [!NOTE|metadata]
> Obsidian-style pipe metadata syntax (Q7-deferred).

Per CALL-E-5 in `docs/spec/callouts.md`: this renders as a plain blockquote (multi-bar chrome), NOT as an unknown-type callout. Forward-compatible — pipe syntax can land later.
