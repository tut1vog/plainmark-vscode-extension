> Plain blockquote — should render with the blockquote multi-bar chrome (one stacked bar at depth 1).
> Second line of plain quote.

> [!NOTE]
> Callout — should NOT show the blockquote bar. Only the callout chrome (accent border + tinted background).

> Another plain blockquote after the callout. Multi-bar chrome returns.
