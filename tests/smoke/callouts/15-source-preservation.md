> [!NOTE] Source preservation roundtrip
> Open this file, do not edit, save, then run `git diff` — zero changes.

> [!TIP]+ Custom title with fold
> Type + fold marker + custom title — all bytes preserved.

> [!UNKNOWN]
> Unknown type — bytes preserved verbatim. Future catalog expansion is additive.

> [!NOTE]
> Multi-paragraph with blank `>` line.
>
> Second paragraph.

```text
> [!NOTE]
inside a fenced code block — should render as raw code, no callout chrome
```

Inline `> [!NOTE]` inside a paragraph (not at line start) — no callout.
