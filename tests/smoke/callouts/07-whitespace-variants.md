>[!NOTE]
> No space after the marker `>`.

>  [!TIP]
> Two spaces after the marker `>`.

> [!CAUTION]   
> Trailing whitespace after the type marker (should render with no custom title — pure type label).
